{
  "name": "poetic-storytelling-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready MERN setup for a poetic storytelling platform.",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "test": "npm run test --prefix server",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "seed:admin": "npm run seed:admin --prefix server",
    "seed:content": "npm run seed:content --prefix server",
    "db:reset": "npm run db:reset --prefix server",
    "repair:indexes": "npm run repair:indexes --prefix server"
  }
}
